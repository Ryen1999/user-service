<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.userservice.dao.BonusEventLogDao">
    <insert id="insertBonusEventLog">
        insert into bonus_event_log(id,biz_id,user_id, value, event, create_time, description) values (#{id},#{bizId},#{userId}, #{value}, #{event}, #{createTime}, #{description})
    </insert>
</mapper>